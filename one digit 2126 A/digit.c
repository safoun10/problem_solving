#include <stdio.h>

int main()
{
    int limit;
    scanf("%d", &limit);
    for (int i = 1; i <= limit; i++)
    {
        char num[10], result;
        scanf("%s", &num);

        printf("%c\n", result);
    }

    return 0;
}

// unfinished and unsolved problem :(